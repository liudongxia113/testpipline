# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: |
    docker pull nacos/nacos-server:v3.1.0
  displayName: 'pull docker from nacos'

- task: wiz@1
  inputs:
    serviceConnectionId: 'wiz-qdts'
    command: 'scan'
    scanType: 'containerimage'
    image: 'nacos/nacos-server:v3.1.0'
    byPolicyHits: DISABLED
# - task: JFrogDocker@1
#   inputs:
#    command: 'Push'
#    artifactoryConnection: 'QDTS_Artifactory_V2'
#    imageName: 'cat-docker.artifacts.cat.com/chinaqdts/nacos-server:latest'
#    threads: '3'
#    skipLogin: false
